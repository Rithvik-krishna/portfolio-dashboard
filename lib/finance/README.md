# Financial Data Services

This directory will house server-side financial data fetchers:
- `yahoo.ts`: Integration with Yahoo Finance to fetch Current Market Price (CMP).
- `google.ts`: Integration with Google Finance for P/E ratios and latest earnings data.
- `cache.ts`: Server-side in-memory caching and rate-limiting to prevent rate blocks.
