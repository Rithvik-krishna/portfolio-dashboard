# Dashboard Components

This directory will contain portfolio dashboard UI components:
- `PortfolioTable`: Tabular view of all holdings with conditional formatting (gain/loss colors).
- `SectorBreakdown`: Sector-grouped views and summary cards.
- `DashboardHeader`: Real-time status indicator, refresh controls, and summary totals.
