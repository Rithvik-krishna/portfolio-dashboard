# Chart Components

This directory will contain Recharts-based data visualization components:
- `SectorAllocationChart`: Donut/pie chart for sector weight breakdown.
- `PerformanceChart`: Visual representation of portfolio gain/loss across holdings.
